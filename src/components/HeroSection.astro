---
/**
 * HeroSection — Immersive hero with a cinematic layered background.
 */
---

<section class="hero" aria-label="Welcome to Moonbloom Farm">
  <div class="hero-bg" aria-hidden="true"></div>
  <div class="hero-grain" aria-hidden="true"></div>

  <div class="hero-content container">
    <p class="hero-eyebrow">Glen Ellen, California</p>
    <h1 class="hero-title">
      Moonbloom Farm
      <span class="hero-title-sub">Grown with love in the Valley of the Moon.</span>
    </h1>

    <div class="hero-cta-row">
      <a href="/contact" class="hero-cta hero-cta--primary">Visit the Stand</a>
      <a href="/what-we-sell" class="hero-cta hero-cta--ghost">See the Harvest</a>
    </div>

    <ul class="hero-facts" aria-label="Farm highlights">
      <li><span>Since 2023</span></li>
      <li><span>Sustainably Grown</span></li>
      <li><span>Sonoma Valley</span></li>
    </ul>
  </div>

  <div class="scroll-indicator" aria-hidden="true">
    <span class="scroll-text">Scroll</span>
    <svg
      class="scroll-chevron"
      width="24"
      height="24"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
    >
      <polyline points="6 9 12 15 18 9"></polyline>
    </svg>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    margin-top: calc(-1 * (var(--space-sm) * 2 + 1.5rem * 1.2));
    padding-top: calc(var(--space-sm) * 2 + 1.5rem * 1.2);
    border-bottom: 1px solid rgba(243, 237, 225, 0.2);
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    background:
      linear-gradient(120deg, rgba(35, 30, 24, 0.84), rgba(48, 42, 32, 0.72)),
      url('/images/hero-bg.svg');
    background-size: cover;
    background-position: center;
  }

  .hero-grain {
    position: absolute;
    inset: 0;
    z-index: 1;
    background-image:
      radial-gradient(circle at 10% 20%, rgba(201, 169, 106, 0.25), transparent 30%),
      radial-gradient(circle at 88% 78%, rgba(178, 120, 85, 0.22), transparent 38%);
    mix-blend-mode: screen;
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 920px;
    padding-top: var(--space-xl);
    padding-bottom: var(--space-xl);
  }

  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 0.72rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    font-weight: 700;
    color: var(--color-brass-soft);
    margin: 0 0 var(--space-sm);
  }

  .hero-eyebrow::before {
    content: '';
    display: block;
    width: 42px;
    height: 1px;
    background-color: currentColor;
    opacity: 0.75;
  }

  .hero-title {
    margin: 0;
    max-width: 14ch;
    font-family: var(--font-heading);
    font-variation-settings: 'opsz' 144, 'SOFT' 30;
    font-size: clamp(3rem, 9vw, 6.7rem);
    font-weight: 500;
    line-height: 0.96;
    letter-spacing: -0.012em;
    color: var(--color-paper);
    text-wrap: balance;
  }

  .hero-title-sub {
    display: block;
    margin-top: var(--space-md);
    max-width: 28ch;
    font-family: var(--font-heading);
    font-style: italic;
    font-variation-settings: 'opsz' 18, 'SOFT' 50;
    font-size: clamp(1.1rem, 2.1vw, 1.4rem);
    font-weight: 400;
    line-height: 1.5;
    letter-spacing: 0;
    color: rgba(243, 237, 225, 0.78);
  }

  .hero-cta-row {
    margin-top: var(--space-lg);
    display: flex;
    gap: 0.8rem;
    flex-wrap: wrap;
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.88rem 1.35rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-size: 0.74rem;
    font-weight: 700;
    transition: transform var(--transition), background-color var(--transition), color var(--transition), border-color var(--transition);
  }

  .hero-cta--primary {
    background-color: var(--color-brass);
    border: 1px solid var(--color-brass);
    color: #1f1a15;
  }

  .hero-cta--primary:hover {
    background-color: var(--color-brass-soft);
    border-color: var(--color-brass-soft);
    color: #1f1a15;
    transform: translateY(-2px);
  }

  .hero-cta--ghost {
    border: 1px solid rgba(243, 237, 225, 0.42);
    color: var(--color-paper);
    background-color: rgba(243, 237, 225, 0.08);
  }

  .hero-cta--ghost:hover {
    border-color: var(--color-paper);
    color: var(--color-paper);
    background-color: rgba(243, 237, 225, 0.16);
    transform: translateY(-2px);
  }

  .hero-facts {
    margin: var(--space-xl) 0 0;
    padding: 0;
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1.4rem;
  }

  .hero-facts li {
    position: relative;
    padding: 0;
  }

  .hero-facts li + li::before {
    content: '';
    position: absolute;
    left: -0.78rem;
    top: 50%;
    width: 3px;
    height: 3px;
    border-radius: 50%;
    background-color: rgba(216, 190, 138, 0.65);
    transform: translateY(-50%);
  }

  .hero-facts span {
    color: rgba(243, 237, 225, 0.78);
    text-transform: uppercase;
    letter-spacing: 0.24em;
    font-size: 0.66rem;
    font-weight: 600;
  }

  .scroll-indicator {
    position: absolute;
    left: 50%;
    bottom: var(--space-md);
    transform: translateX(-50%);
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.3rem;
    color: rgba(243, 237, 225, 0.68);
  }

  .scroll-text {
    font-size: 0.62rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .scroll-chevron {
    animation: bob 2s ease-in-out infinite;
  }

  @keyframes bob {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(5px);
    }
  }

  @media (max-width: 900px) {
    .hero-title {
      max-width: 12ch;
    }
  }

  @media (max-width: 640px) {
    .hero {
      min-height: 100svh;
    }

    .hero-content {
      padding-top: var(--space-lg);
      padding-bottom: var(--space-lg);
    }

    .hero-facts {
      gap: 1rem;
      flex-direction: column;
      align-items: flex-start;
    }

    .hero-facts li + li::before {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-cta,
    .scroll-chevron {
      transition: none;
      animation: none;
    }
  }
</style>
